<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/b6ef7650fd.js" crossorigin="anonymous"></script>
    <title>Main | <%=title%>
    </title>
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        body {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        form{
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-top: 15px;
        }
        .header {
            width: 60%;
            height: 70px;
            padding: 20px;
            background-color: #ccc;
            font-size: 25px;
            display: flex;
            align-items: center;
            justify-content: space-around;
        }

        .header input {
            width: 500px;
        }

        .header input,
        button {
            padding: 5px 8px;
        }

        .headericons {
            width: 20%;
            display: flex;
            justify-content: space-around;
        }
        #get-post, #posts{
            width: 60%;
            
        }
        #title, #review{
            width: 100%;
            padding: 10px 20px;
        }
        #get-post-btn{
            width: 25%;
            align-self: flex-end;
            color: white;
            background-color: blue;
        }
        #post-ares{
            text-indent: 50px;
        }
        #timeline{
            padding: 10px;
            border: 1px solid #ccc;
            font-size: 20px;
            width: 60%;
        }
    </style>
</head>

<body>
    
    <div class="header">
        <div class="headericons">
            <i class="fa-solid fa-arrow-left fa-xl"></i>
            <i class="fa-solid fa-arrow-right fa-xl"></i>
            <i class="fa-solid fa-xmark fa-xl"></i>
            <i class="fa-solid fa-house-chimney fa-xl"></i>
        </div>

        <input id="search" type="text" placeholder="http://tsociety.io/bootcamp">
        <button value="Submit"><i class="fa-solid fa-magnifying-glass"></i> Submit</button>

    </div>
    <div id="timeline">
        Timeline
    </div>
    <form action="/create-post" method="POST" id="get-post">
        <input type="text" name="title" id="title" placeholder="Your full name" required>
        <textarea name="review" id="review" rows="10" cols="30" minlength="25" placeholder="Type your message here" required></textarea >
        <button type="submit" id="get-post-btn">Post a message</button>
    </form>

    <div id="posts">
        <div id="one-post">
            <% for(let i =0; i< posts.length; i++){ %>
                <p><b><%= posts[i].title %> - <%= posts[i].created_at %></b></p>
                <div>
                    <p id="post-text" style="padding-left: 20px; margin-bottom: 25px;"><%= posts[i].review %></p>
                </div>
            <% } %>
        </div>
    </div>

</body>

</html>